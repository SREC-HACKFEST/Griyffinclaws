<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CropImageActivity"
            android:exported="false" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".activities.StudentsList" />
        <activity android:name=".activities.ModifySubject" />
        <activity android:name=".activities.ModifyStudent" />
        <activity android:name=".activities.ModifyTeacher" />
        <activity android:name=".activities.ListActivity" />
        <activity android:name=".activities.Modifications" />
        <activity android:name=".activities.RegisterTeacher" />
        <activity android:name=".activities.RegisterStudent" />
        <activity android:name=".activities.RegisterSubject" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ChangePassword" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>